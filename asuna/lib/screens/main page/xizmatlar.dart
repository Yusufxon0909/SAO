import 'package:flutter/material.dart';

class MeningOilam extends StatefulWidget {
  const MeningOilam({super.key});

  @override
  State<MeningOilam> createState() => _MeningOilamState();
}

class _MeningOilamState extends State<MeningOilam> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mening Oilam')),
      body: const Center(child: Text('Mening Oilam Screen')),
    );
  }
}

class MeningUyim extends StatefulWidget {
  const MeningUyim({super.key});

  @override
  State<MeningUyim> createState() => _MeningUyimState();
}

class _MeningUyimState extends State<MeningUyim> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mening Uyim')),
      body: const Center(child: Text('Mening Uyim Screen')),
    );
  }
}

class MeningAvto extends StatefulWidget {
  const MeningAvto({super.key});

  @override
  State<MeningAvto> createState() => _MeningAvtoState();
}

class _MeningAvtoState extends State<MeningAvto> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mening Avtomobilim')),
      body: const Center(child: Text('Mening Avtomobilim Screen')),
    );
  }
}

class Xayriya extends StatefulWidget {
  const Xayriya({super.key});

  @override
  State<Xayriya> createState() => _XayriyaState();
}

class _XayriyaState extends State<Xayriya> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Xayriya')),
      body: const Center(child: Text('Xayriya Screen')),
    );
  }
}

class TaomlarYetkazish extends StatefulWidget {
  const TaomlarYetkazish({super.key});

  @override
  State<TaomlarYetkazish> createState() => _TaomlarYetkazishState();
}

class _TaomlarYetkazishState extends State<TaomlarYetkazish> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Taomlar Yetkazish')),
      body: const Center(child: Text('Taomlar Yetkazish Screen')),
    );
  }
}

class AviaChiptalar extends StatefulWidget {
  const AviaChiptalar({super.key});

  @override
  State<AviaChiptalar> createState() => _AviaChiptalarState();
}

class _AviaChiptalarState extends State<AviaChiptalar> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Avia Chiptalar')),
      body: const Center(child: Text('Avia Chiptalar Screen')),
    );
  }
}

class KinoteatrgaChiptalar extends StatefulWidget {
  const KinoteatrgaChiptalar({super.key});

  @override
  State<KinoteatrgaChiptalar> createState() => _KinoteatrgaChiptalarState();
}

class _KinoteatrgaChiptalarState extends State<KinoteatrgaChiptalar> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Kinoteatr Chiptalar')),
      body: const Center(child: Text(' Kinoteatr Chiptalar Screen')),
    );
  }
}

class Poverbanklar extends StatefulWidget {
  const Poverbanklar({super.key});

  @override
  State<Poverbanklar> createState() => _PoverbanklarState();
}

class _PoverbanklarState extends State<Poverbanklar> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Poverbanklar')),
      body: const Center(child: Text('Poverbanklar Screen')),
    );
  }
}
